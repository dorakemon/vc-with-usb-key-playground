import { ServerIcon } from "@/components/Icons";
import { BaseEntity } from "./BaseEntity";

export const ServerEntity = () => (
  <BaseEntity icon={<ServerIcon width="100%" height="100%" />} title="Server" />
);
